import SwiftUI

/// Main app entry point - launches the ContentView which handles update notifications
@main
struct Swift_Update_NotifierApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
